{
  "name": "comparating-template",
  "version": "0.8.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "dev:ssr": "ng run comparating-template:serve-ssr",
    "serve:ssr": "node dist/server/main.js",
    "build:ssr": "ng build --prod && ng run comparating-template:server:production",
    "prerender": "ng run comparating-template:prerender",
    "deploy": "ng build --configuration production && firebase deploy"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
